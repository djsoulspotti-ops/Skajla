<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Studente - SKAILA</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="/static/css/tokens.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: var(--font-sans);
            background: var(--bg-secondary);
        }

        .dashboard-layout {
            display: grid;
            grid-template-columns: 260px 1fr;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            background: white;
            border-right: 1px solid var(--border-primary);
            padding: 2rem 1rem;
            display: flex;
            flex-direction: column;
        }

        .sidebar-brand {
            font-size: var(--text-2xl);
            font-weight: var(--font-extrabold);
            background: linear-gradient(135deg, var(--color-blue-900) 0%, var(--color-blue-700) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 3rem;
            text-align: center;
        }

        .nav-menu {
            list-style: none;
            margin-bottom: auto;
        }

        .nav-item {
            margin-bottom: 0.5rem;
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.875rem 1rem;
            color: var(--text-secondary);
            text-decoration: none;
            border-radius: var(--radius-lg);
            font-size: var(--text-sm);
            font-weight: var(--font-medium);
            transition: all 0.2s ease;
        }

        .nav-link:hover, .nav-link.active {
            background: linear-gradient(135deg, rgba(0, 59, 115, 0.08) 0%, rgba(0, 116, 217, 0.05) 100%);
            color: var(--color-blue-800);
        }

        .nav-link i {
            width: 20px;
            text-align: center;
        }

        .sidebar-footer {
            padding-top: 1.5rem;
            border-top: 1px solid var(--border-primary);
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem;
            background: var(--bg-surface);
            border-radius: var(--radius-lg);
            margin-bottom: 0.75rem;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: var(--radius-full);
            background: linear-gradient(135deg, var(--color-blue-800) 0%, var(--color-blue-600) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: var(--font-bold);
            font-size: var(--text-sm);
        }

        .user-info {
            flex: 1;
            min-width: 0;
        }

        .user-name {
            font-size: var(--text-sm);
            font-weight: var(--font-semibold);
            color: var(--text-primary);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .user-role {
            font-size: var(--text-xs);
            color: var(--text-muted);
        }

        .btn-logout {
            width: 100%;
            padding: 0.75rem;
            background: transparent;
            border: 1px solid var(--border-primary);
            border-radius: var(--radius-lg);
            color: var(--text-secondary);
            font-size: var(--text-sm);
            font-weight: var(--font-medium);
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            text-decoration: none;
        }

        .btn-logout:hover {
            background: rgba(239, 68, 68, 0.05);
            border-color: var(--color-ruby-500);
            color: var(--color-ruby-600);
        }

        /* Main Content */
        .main-content {
            padding: 2rem;
            overflow-y: auto;
        }

        .page-header {
            margin-bottom: 2.5rem;
        }

        .page-title {
            font-size: var(--text-4xl);
            font-weight: var(--font-bold);
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .page-subtitle {
            font-size: var(--text-base);
            color: var(--text-secondary);
        }

        /* KPI Cards */
        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 1.5rem;
            margin-bottom: 2.5rem;
        }

        .kpi-card {
            background: white;
            border: 1px solid var(--border-primary);
            border-radius: var(--radius-xl);
            padding: 1.5rem;
            transition: all 0.2s ease;
        }

        .kpi-card:hover {
            border-color: var(--color-blue-600);
            box-shadow: var(--shadow-card-hover);
            transform: translateY(-2px);
        }

        .kpi-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1rem;
        }

        .kpi-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, var(--color-blue-900) 0%, var(--color-blue-700) 100%);
            border-radius: var(--radius-lg);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.25rem;
        }

        .kpi-value {
            font-size: var(--text-4xl);
            font-weight: var(--font-bold);
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .kpi-label {
            font-size: var(--text-sm);
            color: var(--text-muted);
            font-weight: var(--font-medium);
        }

        .kpi-trend {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            font-size: var(--text-xs);
            font-weight: var(--font-semibold);
            padding: 0.25rem 0.5rem;
            border-radius: var(--radius-full);
            margin-top: 0.5rem;
        }

        .kpi-trend.positive {
            background: rgba(16, 185, 129, 0.1);
            color: var(--color-emerald-600);
        }

        .kpi-trend.negative {
            background: rgba(239, 68, 68, 0.1);
            color: var(--color-ruby-600);
        }

        /* Content Grid */
        .content-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .card {
            background: white;
            border: 1px solid var(--border-primary);
            border-radius: var(--radius-xl);
            padding: 1.5rem;
        }

        .card-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1.5rem;
        }

        .card-title {
            font-size: var(--text-xl);
            font-weight: var(--font-semibold);
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .card-action {
            font-size: var(--text-sm);
            color: var(--color-blue-700);
            text-decoration: none;
            font-weight: var(--font-medium);
            transition: color 0.2s ease;
        }

        .card-action:hover {
            color: var(--color-blue-900);
            text-decoration: underline;
        }

        /* Quick Actions */
        .actions-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
        }

        .action-card {
            background: var(--bg-surface);
            border: 1px solid var(--border-primary);
            border-radius: var(--radius-lg);
            padding: 1.25rem;
            text-align: center;
            text-decoration: none;
            transition: all 0.2s ease;
        }

        .action-card:hover {
            border-color: var(--color-blue-600);
            background: white;
            transform: translateY(-2px);
        }

        /* Disabled feature styles */
        .action-card.disabled,
        .nav-link.disabled {
            opacity: 0.5;
            cursor: not-allowed;
            background: var(--bg-tertiary);
            pointer-events: none;
            position: relative;
        }

        .action-card.disabled::after,
        .nav-link.disabled::after {
            content: '🔒';
            position: absolute;
            top: 8px;
            right: 8px;
            font-size: 0.875rem;
        }

        .feature-badge {
            display: inline-block;
            padding: 2px 8px;
            background: linear-gradient(135deg, var(--color-gold-600) 0%, var(--color-gold-500) 100%);
            color: white;
            border-radius: var(--radius-full);
            font-size: 0.625rem;
            font-weight: var(--font-bold);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-left: 0.5rem;
        }

        .disabled-tooltip {
            position: relative;
            display: inline-block;
        }

        .disabled-tooltip:hover::before {
            content: attr(data-tooltip);
            position: absolute;
            bottom: 100%;
            left: 50%;
            transform: translateX(-50%);
            padding: 0.5rem 0.75rem;
            background: var(--color-slate-900);
            color: white;
            font-size: var(--text-xs);
            border-radius: var(--radius-md);
            white-space: nowrap;
            z-index: 1000;
            margin-bottom: 0.5rem;
        }

        .action-icon {
            font-size: 2rem;
            color: var(--color-blue-700);
            margin-bottom: 0.75rem;
        }

        .action-card.disabled .action-icon {
            color: var(--text-muted);
        }

        .action-title {
            font-size: var(--text-sm);
            font-weight: var(--font-semibold);
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .action-desc {
            font-size: var(--text-xs);
            color: var(--text-muted);
        }

        /* Activity List */
        .activity-list {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .activity-item {
            display: flex;
            gap: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--border-secondary);
        }

        .activity-item:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .activity-icon-wrapper {
            width: 40px;
            height: 40px;
            background: var(--bg-surface);
            border-radius: var(--radius-lg);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .activity-icon-wrapper i {
            color: var(--color-blue-700);
        }

        .activity-content {
            flex: 1;
        }

        .activity-title {
            font-size: var(--text-sm);
            font-weight: var(--font-semibold);
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .activity-desc {
            font-size: var(--text-xs);
            color: var(--text-muted);
        }

        .activity-time {
            font-size: var(--text-xs);
            color: var(--text-muted);
            text-align: right;
        }

        /* Responsive */
        @media (max-width: 1280px) {
            .kpi-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .content-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 1024px) {
            .dashboard-layout {
                grid-template-columns: 1fr;
            }
            .sidebar {
                display: none;
            }
        }

        @media (max-width: 640px) {
            .kpi-grid {
                grid-template-columns: 1fr;
            }
            .actions-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
    
    <!-- Circulating Avatars Component -->
    <link rel="stylesheet" href="/static/css/circulating-avatars.css">
</head>
<body>
    <div class="dashboard-layout">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-brand">SKAILA</div>
            
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="/dashboard" class="nav-link active">
                        <i class="fas fa-home"></i>
                        <span>Dashboard</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/calendario" class="nav-link {% if not enabled_features.get('calendario', True) %}disabled{% endif %}" title="{% if not enabled_features.get('calendario', True) %}Disponibile nel piano Premium{% endif %}">
                        <i class="fas fa-calendar"></i>
                        <span>Calendario</span>
                        {% if not enabled_features.get('calendario', True) %}<span class="feature-badge">Premium</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/registro" class="nav-link {% if not enabled_features.get('registro_elettronico', True) %}disabled{% endif %}" title="{% if not enabled_features.get('registro_elettronico', True) %}Disponibile nel piano Premium{% endif %}">
                        <i class="fas fa-book"></i>
                        <span>Registro</span>
                        {% if not enabled_features.get('registro_elettronico', True) %}<span class="feature-badge">Premium</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/materiali" class="nav-link {% if not enabled_features.get('materiali_didattici', True) %}disabled{% endif %}" title="{% if not enabled_features.get('materiali_didattici', True) %}Disponibile nel piano Premium{% endif %}">
                        <i class="fas fa-folder"></i>
                        <span>Materiali</span>
                        {% if not enabled_features.get('materiali_didattici', True) %}<span class="feature-badge">Premium</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/chat" class="nav-link">
                        <div id="messaging-avatars-container" class="circulating-avatars-container" style="display: inline-block;">
                            <i class="fas fa-comments"></i>
                        </div>
                        <span>Messaggi</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/ai-chat" class="nav-link {% if not enabled_features.get('ai_coach', True) %}disabled{% endif %}" title="{% if not enabled_features.get('ai_coach', True) %}Disponibile nel piano Premium{% endif %}">
                        <i class="fas fa-robot"></i>
                        <span>AI Coach</span>
                        {% if not enabled_features.get('ai_coach', True) %}<span class="feature-badge">Premium</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/gamification" class="nav-link {% if not enabled_features.get('gamification', True) %}disabled{% endif %}" title="{% if not enabled_features.get('gamification', True) %}Disponibile nel piano Premium{% endif %}">
                        <i class="fas fa-gamepad"></i>
                        <span>Gamification</span>
                        {% if not enabled_features.get('gamification', True) %}<span class="feature-badge">Premium</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/skaila-connect" class="nav-link {% if not enabled_features.get('skaila_connect', True) %}disabled{% endif %}" title="{% if not enabled_features.get('skaila_connect', True) %}Disponibile nel piano Premium{% endif %}">
                        <i class="fas fa-briefcase"></i>
                        <span>SKAILA Connect</span>
                        {% if not enabled_features.get('skaila_connect', True) %}<span class="feature-badge">Premium</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/crediti" class="nav-link {% if not enabled_features.get('gamification', True) %}disabled{% endif %}">
                        <i class="fas fa-coins"></i>
                        <span>I Miei Crediti</span>
                    </a>
                </li>
            </ul>

            <div class="sidebar-footer">
                <div class="user-profile">
                    <div class="user-avatar">{{ user.nome[0]|upper }}{{ user.cognome[0]|upper }}</div>
                    <div class="user-info">
                        <div class="user-name">{{ user.nome }} {{ user.cognome }}</div>
                        <div class="user-role">Studente</div>
                    </div>
                </div>
                <a href="/logout" class="btn-logout">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Esci</span>
                </a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <div class="page-header">
                <h1 class="page-title">Benvenuto, {{ user.nome }}!</h1>
                <p class="page-subtitle">Ecco un riepilogo delle tue attività e progressi</p>
            </div>

            <!-- KPI Cards -->
            <div class="kpi-grid">
                <div class="kpi-card">
                    <div class="kpi-header">
                        <div class="kpi-icon">
                            <i class="fas fa-chart-line"></i>
                        </div>
                    </div>
                    <div class="kpi-value">{{ gamification.livello|default(1) }}</div>
                    <div class="kpi-label">Livello Attuale</div>
                    <span class="kpi-trend positive">
                        <i class="fas fa-arrow-up"></i> +2 questo mese
                    </span>
                </div>

                <div class="kpi-card">
                    <div class="kpi-header">
                        <div class="kpi-icon">
                            <i class="fas fa-star"></i>
                        </div>
                    </div>
                    <div class="kpi-value">{{ gamification.xp_totale|default(0) }}</div>
                    <div class="kpi-label">Punti Esperienza</div>
                    <span class="kpi-trend positive">
                        <i class="fas fa-arrow-up"></i> +150 XP
                    </span>
                </div>

                <div class="kpi-card">
                    <div class="kpi-header">
                        <div class="kpi-icon">
                            <i class="fas fa-graduation-cap"></i>
                        </div>
                    </div>
                    <div class="kpi-value">8.5</div>
                    <div class="kpi-label">Media Voti</div>
                    <span class="kpi-trend positive">
                        <i class="fas fa-arrow-up"></i> +0.3
                    </span>
                </div>

                <div class="kpi-card">
                    <div class="kpi-header">
                        <div class="kpi-icon">
                            <i class="fas fa-check-circle"></i>
                        </div>
                    </div>
                    <div class="kpi-value">95%</div>
                    <div class="kpi-label">Presenze</div>
                    <span class="kpi-trend positive">
                        <i class="fas fa-arrow-up"></i> Ottimo
                    </span>
                </div>
            </div>

            <!-- Study Timer - Full Width -->
            {% include 'partials/study_timer.html' %}

            <!-- Content Grid -->
            <div class="content-grid">
                <!-- Left Column -->
                <div>
                    <div class="card">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-bolt"></i>
                                Azioni Rapide
                            </h2>
                        </div>
                        <div class="actions-grid">
                            <a href="/quiz" class="action-card">
                                <div class="action-icon"><i class="fas fa-brain"></i></div>
                                <div class="action-title">Quiz Adattivo</div>
                                <p class="action-desc">Inizia un quiz personalizzato</p>
                            </a>
                            <a href="/materiali" class="action-card">
                                <div class="action-icon"><i class="fas fa-folder-open"></i></div>
                                <div class="action-title">Materiali</div>
                                <p class="action-desc">Accedi ai materiali didattici</p>
                            </a>
                            <a href="/chat" class="action-card">
                                <div class="action-icon"><i class="fas fa-comments"></i></div>
                                <div class="action-title">Messaggi</div>
                                <p class="action-desc">Comunicazioni con docenti</p>
                            </a>
                            <a href="/calendario" class="action-card">
                                <div class="action-icon"><i class="fas fa-calendar-alt"></i></div>
                                <div class="action-title">Calendario</div>
                                <p class="action-desc">Vedi lezioni ed eventi</p>
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Right Column -->
                <div>
                    <!-- Gamification Quick Widget -->
                    <div class="card" style="margin-bottom: 1.5rem;">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-gamepad"></i>
                                Gamification
                            </h2>
                            <a href="/gamification" class="card-action">Dashboard Completa →</a>
                        </div>
                        <div style="padding-top: 1rem;">
                            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-bottom: 1rem;">
                                <div style="text-align: center; padding: 1rem; background: var(--bg-surface); border-radius: var(--radius-lg);">
                                    <div style="font-size: 2rem; font-weight: bold; color: var(--color-blue-800);">{{ gamification.profile.current_level|default(1) }}</div>
                                    <div style="font-size: 0.85rem; color: var(--text-muted);">Livello</div>
                                </div>
                                <div style="text-align: center; padding: 1rem; background: var(--bg-surface); border-radius: var(--radius-lg);">
                                    <div style="font-size: 2rem; font-weight: bold; color: var(--color-blue-800);">{{ gamification.profile.total_xp|default(0) }}</div>
                                    <div style="font-size: 0.85rem; color: var(--text-muted);">XP Totali</div>
                                </div>
                            </div>
                            <div style="background: var(--bg-surface); border-radius: 10px; padding: 0.75rem; margin-top: 1rem;">
                                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem;">
                                    <span style="font-size: 0.9rem; color: var(--text-secondary);">Progresso Livello</span>
                                    <span style="font-size: 0.9rem; font-weight: bold; color: var(--color-blue-800);">{{ gamification.level_info.progress_percent|default(0) }}%</span>
                                </div>
                                <div style="background: var(--border-secondary); height: 8px; border-radius: 4px; overflow: hidden;">
                                    <div style="background: linear-gradient(135deg, var(--color-blue-800) 0%, var(--color-blue-600) 100%); height: 100%; width: {{ gamification.level_info.progress_percent|default(0) }}%; border-radius: 4px;"></div>
                                </div>
                            </div>
                            <a href="/gamification" style="display: block; margin-top: 1rem; text-align: center; background: var(--color-blue-800); color: white; padding: 0.75rem; border-radius: 8px; text-decoration: none; font-weight: 600; transition: all 0.2s;">
                                🏆 Vedi Badge, Achievement & Classifica
                            </a>
                        </div>
                    </div>

                    <div class="card">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-history"></i>
                                Attività Recenti
                            </h2>
                            <a href="#" class="card-action">Vedi tutte</a>
                        </div>
                        <div class="activity-list">
                            {% if recent_activities and recent_activities|length > 0 %}
                                {% for activity in recent_activities %}
                                <div class="activity-item">
                                    <div class="activity-icon-wrapper">
                                        {% if 'quiz' in activity.action_type.lower() %}
                                            <i class="fas fa-brain"></i>
                                        {% elif 'messag' in activity.action_type.lower() or 'chat' in activity.action_type.lower() %}
                                            <i class="fas fa-comment"></i>
                                        {% elif 'badge' in activity.action_type.lower() or 'achievement' in activity.action_type.lower() %}
                                            <i class="fas fa-trophy"></i>
                                        {% elif 'login' in activity.action_type.lower() %}
                                            <i class="fas fa-sign-in-alt"></i>
                                        {% else %}
                                            <i class="fas fa-star"></i>
                                        {% endif %}
                                    </div>
                                    <div class="activity-content">
                                        <div class="activity-title">{{ activity.title }}</div>
                                        <p class="activity-desc">{{ activity.description }}{% if activity.xp_earned and activity.xp_earned > 0 %} - +{{ activity.xp_earned }} XP{% endif %}</p>
                                    </div>
                                    <div class="activity-time">
                                        {% if activity.timestamp.hour is defined %}
                                            {{ activity.timestamp.strftime('%d/%m %H:%M') }}
                                        {% else %}
                                            {{ activity.timestamp.strftime('%d/%m') }}
                                        {% endif %}
                                    </div>
                                </div>
                                {% endfor %}
                            {% else %}
                                <div style="text-align: center; padding: 2rem; color: var(--text-muted);">
                                    <i class="fas fa-inbox" style="font-size: 2rem; margin-bottom: 0.5rem; opacity: 0.3;"></i>
                                    <p>Nessuna attività recente</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>
    
    <!-- Socket.IO and Circulating Avatars -->
    <script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>
    <script src="/static/js/circulating-avatars.js"></script>
    <script>
        // Initialize Socket.IO connection
        window.socket = io({
            transports: ['websocket', 'polling'],
            reconnection: true,
            reconnectionAttempts: 5,
            reconnectionDelay: 1000
        });
        
        socket.on('connect', function() {
            console.log('✅ Socket.IO connected');
        });
        
        socket.on('disconnect', function() {
            console.log('⚠️ Socket.IO disconnected');
        });
        
        // Initialize Circulating Avatars around messaging button
        document.addEventListener('DOMContentLoaded', function() {
            if (document.getElementById('messaging-avatars-container')) {
                window.messagingAvatars = new CirculatingAvatars('messaging-avatars-container', {
                    maxAvatars: 7,
                    orbitRadius: 45,
                    avatarSize: 26,
                    animationDuration: 15,
                    updateInterval: 30000
                });
                
                console.log('✅ Circulating avatars initialized');
            }
        });
    </script>
</body>
</html>
